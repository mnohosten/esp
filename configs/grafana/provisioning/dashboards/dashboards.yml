apiVersion: 1

providers:
  - name: 'ESP Dashboards'
    orgId: 1
    folder: 'ESP'
    folderUid: esp
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
